// import { addBetOption } from './betOption'

describe('Betptions', () => {
  describe('when calling addBetOptiopns', () => {
    it('should add a new betoption into betoptions document', () => {
      expect(0).toBe(0)
    })
  })
})
